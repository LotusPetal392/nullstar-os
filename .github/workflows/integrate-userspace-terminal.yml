name: Integrate blocking userspace terminal

on:
  push:
    branches:
      - agent/userspace-terminal

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v7
        with:
          ref: agent/userspace-terminal

      - name: Install pinned Rust toolchain
        run: |
          rustup -q toolchain install nightly-2026-02-01 \
            --profile minimal \
            --component llvm-tools-preview \
            --component rust-src \
            --component rustfmt
          rustup -q target add x86_64-unknown-none --toolchain nightly-2026-02-01

      - name: Apply terminal milestone
        shell: bash
        run: |
          set -o pipefail
          python3 tools/apply_userspace_terminal.py 2>&1 | tee userspace-terminal-integration.log

      - name: Format sources
        run: cargo +nightly-2026-02-01 fmt --all

      - name: Generate workspace lockfile
        run: cargo +nightly-2026-02-01 generate-lockfile

      - name: Commit integrated sources
        run: |
          git config user.name "lotuspetalagent"
          git config user.email "lotuspetalagent@users.noreply.github.com"
          git add Cargo.lock build.rs kernel/src src/main.rs userspace
          if ! git diff --cached --quiet; then
            git commit -m "Add blocking userspace terminal input"
            git push origin HEAD:agent/userspace-terminal
          fi

      - name: Build bootable image
        shell: bash
        run: |
          set -o pipefail
          cargo +nightly-2026-02-01 build --locked 2>&1 | tee userspace-terminal-build.log

      - name: Upload integration diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: userspace-terminal-integration-log
          path: userspace-terminal-integration.log
          if-no-files-found: ignore

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: userspace-terminal-build-log
          path: userspace-terminal-build.log
          if-no-files-found: ignore
