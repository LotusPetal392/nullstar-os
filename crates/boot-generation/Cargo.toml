[package]
name = "boot-generation"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/LotusPetal392/nullstar-os"

[dependencies]
nullfs-format = { path = "../nullfs-format" }

[lib]
path = "src/lib.rs"
