[package]
name = "nswp-logging"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/LotusPetal392/nullstar-os"

[dependencies]
nswp-core = { path = "../nswp-core" }
nswp-runtime = { path = "../nswp-runtime" }
service-route = { path = "../service-route" }

[lib]
path = "src/lib.rs"
