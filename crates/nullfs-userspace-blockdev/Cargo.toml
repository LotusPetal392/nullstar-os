[package]
name = "nullfs-userspace-blockdev"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/LotusPetal392/nullstar-os"

[dependencies]
nullfs-blockdev = { path = "../nullfs-blockdev", default-features = false }
userspace = { path = "../../userspace" }

[lib]
path = "src/lib.rs"
