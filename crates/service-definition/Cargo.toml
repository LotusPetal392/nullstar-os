[package]
name = "service-definition"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/LotusPetal392/nullstar-os"

[lib]
path = "src/lib.rs"

[dependencies]
service-route = { path = "../service-route" }
